# Shared body for exact-match /api/* locations (rate-limited endpoints).
# Kept in one place so every sensitive location proxies identically.
rewrite ^/api/(.*) /$1 break;
proxy_pass http://backend;
include /etc/nginx/conf.d/proxy_params.conf;
